# 完整建模思路文档

> **文档说明**：本文档整理了 MCM 2026 Problem C "与星共舞" (DWTS) 比赛的完整建模思路，包含每道题的建模方法、公式推导及所有参数的详细解释。
>
> **题目背景**：美国电视节目《与星共舞》是一场名人与专业舞者搭档的舞蹈比赛。比赛结果由专业评委（1-10分）和观众投票共同决定。历史上节目组曾多次修改规则（如排名制、百分比制、评委拯救制），以平衡"技术性"与"娱乐性"。
>
> **核心挑战**：观众投票数据是保密的，我们需要通过评委分数和淘汰结果反向推算观众投票分布，并设计更公平的赛制。

---

## 目录

1. [问题定义与研究框架](#1-问题定义与研究框架)
2. [Question 1：重建"幽灵"数据](#2-question-1重建幽灵数据)
3. [Question 2：规则对比与反事实分析](#3-question-2规则对比与反事实分析)
4. [Question 3：成功因素分析](#4-question-3成功因素分析)
5. [Question 4：新赛制设计](#5-question-4新赛制设计)
6. [附录：关键指标定义](#附录关键指标定义)

---

## 1. 问题定义与研究框架

### 1.1 核心研究问题

| 问题编号 | 研究问题 | 建模目标 |
|---------|---------|---------|
| **Q1** | 观众投票分布是什么？ | 反向推算每周各选手的粉丝投票份额 |
| **Q2** | 哪种规则更偏向粉丝投票？ | 对比三种规则系统的影响力差异 |
| **Q3** | 哪些因素影响选手表现？ | 分析舞伴、行业、年龄等因素的效应 |
| **Q4** | 如何设计更公平的赛制？ | 提出并验证新的淘汰机制 |

### 1.2 三种历史规则系统

#### 规则 A：排名加和法 (Rank System)
**适用赛季**：第 1-2 季，第 28+ 季（部分）

**公式**：
$$ \text{TotalRank}_i = \text{JudgeRank}_i + \text{FanRank}_i $$

**淘汰规则**：
- $\text{Loser} = \arg\max_i (\text{TotalRank}_i)$
- 平局时：粉丝排名较差者淘汰

**参数说明**：
- $\text{JudgeRank}_i$：选手 $i$ 的评委分排名（1 = 最高分）
- $\text{FanRank}_i$：选手 $i$ 的粉丝投票排名（1 = 最多票）
- 特点：排名是线性的，分差大小不重要，只看相对位置

---

#### 规则 B：百分比加和法 (Percent System)
**适用赛季**：第 3-27 季

**公式**：
$$ \text{TotalScore}_i = \text{JudgeShare}_i + \text{FanShare}_i $$

$$ \text{JudgeShare}_i = \frac{\text{JudgeScore}_i}{\sum_{j=1}^{n} \text{JudgeScore}_j} \times 100\% $$

$$ \text{FanShare}_i = \frac{\text{FanVotes}_i}{\sum_{j=1}^{n} \text{FanVotes}_j} \times 100\% $$

**淘汰规则**：
- $\text{Loser} = \arg\min_i (\text{TotalScore}_i)$

**参数说明**：
- $\text{JudgeScore}_i$：选手 $i$ 的评委总分（多支舞蹈之和）
- $\text{FanVotes}_i$：选手 $i$ 的观众投票数（保密）
- $n$：当周参赛选手数
- 特点：对分差敏感，极低评委分需要巨量粉丝票才能弥补

---

#### 规则 C：排名+评委拯救制 (Rank With Save)
**适用赛季**：第 28 季及以后

**步骤**：
1. 先按规则 A 算出 $\text{TotalRank}$，找出倒数两名（Bottom Two）
2. 评委在直播中投票决定 Bottom Two 中谁留下

**公式**：
$$ P(\text{Saved}_i) = \begin{cases} \text{MERIT\_SAVE\_PROB} & \text{if JudgeScore}_i > \text{JudgeScore}_j \\ 1 - \text{MERIT\_SAVE\_PROB} & \text{if JudgeScore}_i < \text{JudgeScore}_j \end{cases} $$

**参数说明**：
- $\text{MERIT\_SAVE\_PROB} = 0.775$：历史分析得出的评委理性拯救概率
- 评委分数较高者有 77.5% 概率被拯救

---

## 2. Question 1：重建"幽灵"数据

### 2.1 问题定义

**核心挑战**：观众投票数据是保密的，我们需要通过评委分数和淘汰结果反向推算。

**建模目标**：
1. 生成一套"估算的观众投票数"
2. 量化模型的不确定性
3. 识别混沌（不可预测）因素的比例

---

### 2.2 贝叶斯-狄利克雷双世界模型

#### 2.2.1 核心思想

将每周的投票结果解构为两个正交分量的混合：

1. **理性分量 (Rational Component)**：由选手的长期历史表现（积累的 $\alpha$）和当周评委打分（Z-Score）共同驱动
2. **混沌分量 (Chaotic Component)**：代表不可预测的社会动力学因素（如粉丝的非理性狂热、同情票）

#### 2.2.2 混合狄利克雷分布模型

**核心公式**：

$$ \vec{V}_{\text{share}} \sim (1 - \lambda) \cdot \text{Dir}(\vec{\alpha}_{\text{skill}}) + \lambda \cdot \text{Dir}(\vec{\alpha}_{\text{chaos}}) $$

**参数说明**：

| 参数 | 定义 | 取值/来源 |
|-----|-----|---------|
| $\vec{V}_{\text{share}}$ | 粉丝投票份额向量（归一化到和为1） | 模型的输出 |
| $\lambda$ | 混沌权重（待求解） | 优化目标，通过网格搜索确定 |
| $\text{Dir}(\cdot)$ | 狄利克雷分布 | 投票生成的概率模型 |
| $\vec{\alpha}_{\text{skill}}$ | 理性/技能驱动的先验参数向量 | 由上周后验 + 当周评委分数更新 |
| $\vec{\alpha}_{\text{chaos}}$ | 混沌分布的先验参数向量 | 由混沌分布类型决定 |

---

#### 2.2.3 先验分布初始化

**公式**：
$$ \vec{\alpha}^{(0)} = \text{initialize\_priors}(\text{contestant\_names}) $$

**代码实现**：
```python
def initialize_priors(contestant_names: List[str]) -> np.ndarray:
    n = len(contestant_names)
    return np.full(n, INITIAL_ALPHA)  # INITIAL_ALPHA = 1.0
```

**参数说明**：
- $\vec{\alpha}^{(0)}$：初始先验向量
- $\text{INITIAL\_ALPHA} = 1.0$：无信息先验（均匀分布倾向）
- $n$：当周参赛选手数量

**狄利克雷参数 $\alpha$ 的物理意义**：
- $\alpha < 1$：分布呈 "U型"，倾向于极端值（少数人拿大头）
- $\alpha = 1$：均匀分布
- $\alpha > 1$：分布呈 "倒U型"，倾向于集中（大家份额接近）

---

#### 2.2.4 先验分布的跨周传递

**公式**：
$$ \vec{\alpha}^{(t)} = \text{align\_prior\_to\_current}(\vec{\alpha}^{(t-1)}, \text{names}^{(t)}) $$

**详细步骤**：

1. **继承上周后验**（继续参赛的选手）：
   $$ \alpha_i^{(t)} = \alpha_i^{(t-1)} \quad \text{if contestant } i \text{ continued} $$

2. **新选手初始化**：
   $$ \alpha_i^{(t)} = \text{INITIAL\_ALPHA} \quad \text{if contestant } i \text{ is new} $$

3. **缩放调整**（保持相对比例）：
   $$ \text{scale\_factor} = \min\left(\frac{1}{\text{remaining\_ratio}}, 1.3\right) $$
   $$ \vec{\alpha}^{(t)} = \vec{\alpha}^{(t)} \times \text{scale\_factor} $$
   $$ \vec{\alpha}^{(t)} = \max(\vec{\alpha}^{(t)}, \text{MIN\_ALPHA}) $$

**参数说明**：

| 参数 | 定义 | 取值 |
|-----|-----|-----|
| $\vec{\alpha}^{(t)}$ | 第 $t$ 周的先验向量 | 模型状态变量 |
| $\vec{\alpha}^{(t-1)}$ | 第 $t-1$ 周的后验向量 | 模型状态变量 |
| $\text{remaining\_ratio}$ | 选手保留比例 | $n^{(t)} / n^{(t-1)}$ |
| $\text{MIN\_ALPHA}$ | 最小先验值 | 0.1 |

---

#### 2.2.5 评委分数对先验的修正

**公式**：
$$ z_i = \frac{\text{JudgeScore}_i - \mu}{\sigma} $$

$$ \text{multiplier}_i = 1 + \text{SKILL\_IMPACT\_FACTOR} \times z_i $$

$$ \text{multiplier}_i = \text{clip}(\text{multiplier}_i, 0.5, 2.0) $$

$$ \alpha_i^{(t)} = \alpha_i^{(t)} \times \text{multiplier}_i $$

**参数说明**：

| 参数 | 定义 | 取值 |
|-----|-----|-----|
| $\text{JudgeScore}_i$ | 选手 $i$ 当周评委总分 | 原始数据 |
| $\mu$ | 当周所有选手评委分的均值 | $\mu = \frac{1}{n}\sum_{j=1}^{n} \text{JudgeScore}_j$ |
| $\sigma$ | 当周所有选手评委分的标准差 | $\sigma = \sqrt{\frac{1}{n-1}\sum_{j=1}^{n}(\text{JudgeScore}_j - \mu)^2}$ |
| $z_i$ | 选手 $i$ 的 Z-Score（标准化分数） | 衡量相对评委表现 |
| $\text{SKILL\_IMPACT\_FACTOR}$ | 技能影响力系数 | 0.3 |
| $\text{clip}(\cdot, 0.5, 2.0)$ | 截断函数 | 防止极端扭曲 |

**物理意义**：
- Z-Score = 2（极高评委分）：$\text{multiplier} = 1 + 0.3 \times 2 = 1.6$（α 增大 60%）
- Z-Score = -2（极低评委分）：$\text{multiplier} = 1 + 0.3 \times (-2) = 0.4$（α 减小 60%）

---

#### 2.2.6 贝叶斯后验更新

**公式**：
$$ \vec{\alpha}^{(t)}_{\text{posterior}} = \text{update\_prior\_with\_evidence}(\vec{\alpha}^{(t)}_{\text{prior}}, \hat{\vec{V}}_{\text{share}}) $$

$$ \vec{\alpha}^{(t)}_{\text{posterior}} = \vec{\alpha}^{(t)}_{\text{prior}} + \eta \times (\hat{\vec{V}}_{\text{share}} \times \text{EVIDENCE\_BOOST}) $$

$$ \vec{\alpha}^{(t)}_{\text{posterior}} = \max(\vec{\alpha}^{(t)}_{\text{posterior}}, \text{MIN\_ALPHA}) $$

**参数说明**：

| 参数 | 定义 | 取值 |
|-----|-----|-----|
| $\eta$ | 学习率 (LEARNING_RATE) | 0.4 |
| $\hat{\vec{V}}_{\text{share}}$ | 估算的粉丝份额 | 模型输出 |
| $\text{EVIDENCE\_BOOST}$ | 证据增强系数 | 5.0 |

---

#### 2.2.7 混合模型：理性+混沌分量

**公式**：
$$ n_{\text{skill}} = N_{\text{total}} \times (1 - \lambda) $$
$$ n_{\text{chaos}} = N_{\text{total}} \times \lambda $$

$$ \text{samples}_{\text{skill}} \sim \text{Dirichlet}(\vec{\alpha}_{\text{skill}}) $$
$$ \text{samples}_{\text{chaos}} \sim \text{Dirichlet}(\vec{\alpha}_{\text{chaos}}) $$

$$ \text{samples} = \text{concat}(\text{samples}_{\text{skill}}, \text{samples}_{\text{chaos}}) $$

**混沌分布类型**：

| 类型 | 公式 | 物理含义 |
|-----|-----|---------|
| **Uniform** | $\vec{\alpha}_{\text{chaos}} = \vec{1}$ | 完全随机的噪声 |
| **Pareto** | $\alpha_{\text{chaos},i} \propto \text{Pareto}(1.5)$ | 长尾分布，偏向弱者（同情票） |
| **Exponential** | $\alpha_{\text{chaos},i} \propto \text{Exponential}(1.0)$ | 中等尾巴，温和的弱者偏好 |

**参数说明**：

| 参数 | 定义 | 取值 |
|-----|-----|-----|
| $N_{\text{total}}$ | 总模拟次数 | 10,000 |
| $\lambda$ | 混沌比例 | 优化目标（最优 0.024） |
| $\text{CHAOS\_DISTRIBUTION}$ | 混沌分布类型 | 'exponential'（优化结果） |

---

#### 2.2.8 香农熵（不确定性度量）

**公式**：
$$ H_i = -\sum_{k=1}^{50} p_k \ln(p_k) $$

其中 $p_k$ 是选手 $i$ 在第 $k$ 个区间内的投票份额概率。

**参数说明**：
- $H_i$：选手 $i$ 的香农熵（单位：nats）
- bins = 50：将 [0, 1] 区间划分为 50 个桶
- 熵值越高表示模型对投票份额越不确定

---

### 2.3 模型优化：信息比率 (IR)

#### 2.3.1 评价函数

**公式**：
$$ \text{IR}(\lambda) = \frac{\text{Explanation Rate}}{1 - \text{Certainty}} $$

**参数说明**：

| 指标 | 定义 | 计算方式 |
|-----|-----|---------|
| $\text{Explanation Rate}$ | 解释率 | $\frac{\text{成功解释的周数}}{\text{总消除周数}}$ |
| $\text{Certainty}$ | 置信度 | $\frac{n_{\text{valid\_sims}}}{N_{\text{total}}}$ |

#### 2.3.2 优化结果

| 分布模型 | 最佳 $\lambda$ | Information Ratio | 解释率 | 置信度 |
|---------|---------------|-------------------|--------|--------|
| **Exponential** | **0.024** | **1.479** | **95.5%** | **35.4%** |
| Uniform | 0.097 | 1.477 | 97.7% | 33.8% |
| Pareto | 0.017 | 1.473 | 93.9% | 36.2% |

**核心结论**：
- **最优混沌分布**：Exponential（指数分布）
- **最优混沌权重**：$\lambda = 0.024$（2.4%）
- **物理意义**：投票结果由 97.6% 理性因素 + 2.4% 混沌因素决定

---

## 3. Question 2：规则对比与反事实分析

### 3.1 方法论：静态反事实分析

**核心思想**：
- 对历史上的每个周次，保持输入数据不变（评委分数 + Q1 估算的粉丝份额）
- 应用三种不同的淘汰规则，观察"谁会被淘汰"
- **不进行动态推演**（不假设被淘汰选手在后续周次的虚构分数）

---

### 3.2 三种规则的蒙特卡洛实现

#### 3.2.1 排名制 (Rank System)

**公式**：
$$ \text{FanRank}_i = \text{compute\_fan\_rank}(\text{FanShare}_i) $$
$$ \text{TotalRank}_i = \text{JudgeRank}_i + \text{FanRank}_i $$
$$ \text{Eliminated} = \arg\max_i(\text{TotalRank}_i) $$

**平局处理**：
$$ \text{Eliminated} = \arg\max_{\text{TotalRank}_i = \text{TotalRank}_{\max}} (\text{FanRank}_i) $$

**参数说明**：
- $\text{compute\_fan\_rank}$：使用 `method='average'` 处理并列排名

---

#### 3.2.2 百分比制 (Percent System)

**公式**：
$$ \text{JudgeShare}_i = \frac{\text{JudgeScore}_i}{\sum_j \text{JudgeScore}_j} $$
$$ \text{TotalShare}_i = \text{JudgeShare}_i + \text{FanShare}_i $$
$$ \text{Eliminated} = \arg\min_i(\text{TotalShare}_i) $$

---

#### 3.2.3 排名+评委拯救制 (Rank With Save)

**公式**：
1. 计算 $\text{TotalRank}_i = \text{JudgeRank}_i + \text{FanRank}_i$
2. 识别 Bottom Two（排名最高者 = 最差）
3. 评委拯救判定：
   $$ P(\text{Eliminated}_i) = \begin{cases} 1 - \text{MERIT\_SAVE\_PROB} & \text{if JudgeScore}_i < \text{JudgeScore}_j \\ \text{MERIT\_SAVE\_PROB} & \text{if JudgeScore}_i > \text{JudgeScore}_j \end{cases} $$

---

### 3.3 量化评价指标

#### 3.3.1 逆转率 (Reversal Rate)

**公式**：
$$ \text{Reversal Rate} = \frac{\text{规则冲突的周数}}{\text{总周数}} \times 100\% $$

**结果**：13.43%（约 1/7 的周次结果不同）

---

#### 3.3.2 粉丝权重指数 (Fan Power Index, FPI)

**公式**：
$$ \text{FPI} = |\rho(\text{FinalRank}, \text{FanShare})| $$

其中 $\rho$ 是 Spearman 相关系数。

**结果**：

| 规则系统 | FPI | 解读 |
|---------|-----|-----|
| Percent | **0.847** | 最高粉丝影响力 |
| Rank | 0.793 | 中等影响力 |
| Rank+Save | 0.793 | 中等影响力 |

---

#### 3.3.3 平庸存活率 (Mediocrity Survival Rate)

**定义**：评委分数倒数前三名的选手在该规则下的存活率

**公式**：
$$ \text{Mediocrity Survival} = \frac{\text{低分选手存活数}}{\text{低分选手总数}} \times 100\% $$

**结果**：

| 规则系统 | 平庸存活率 | 解读 |
|---------|-----------|-----|
| Percent | 71.09% | 最强"人气保护盾" |
| Rank | 68.30% | 中等保护 |
| **Rank+Save** | **67.40%** | **最有效淘汰低分选手** |

---

#### 3.3.4 英才被杀率 (Talent Elimination Rate)

**定义**：评委分数前三名的选手被淘汰的比例

**公式**：
$$ \text{Talent Elimination} = \frac{\text{高分选手淘汰数}}{\text{高分选手总数}} \times 100\% $$

**结果**：

| 规则系统 | 英才被杀率 | 解读 |
|---------|-----------|-----|
| Percent | 4.50% | 保护最差 |
| Rank | 3.20% | 中等保护 |
| **Rank+Save** | **2.00%** | **最强技术保护** |

---

## 4. Question 3：成功因素分析

### 4.1 双轨归因模型 (Dual-Track Attribution Model)

**核心思想**：将选手表现分解为两个独立赛道

| 赛道 | 因变量 | 衡量内容 |
|-----|-------|---------|
| **技术轨道 (Model A)** | $\text{Score}_{\text{normalized}}$ | 评委评分（舞蹈技术水平） |
| **人气轨道 (Model B)** | $\log(\text{FanShare})$ | 粉丝投票份额（观众喜爱度） |

---

### 4.2 行业聚类 (K-Means Clustering)

#### 4.2.1 三维评分体系

**公式**：
$$ \text{IndustryScore}_i = (\text{Physicality}_i, \text{Performance}_i, \text{Fanbase}_i) $$

**行业代码book示例**：

| 行业 | Physicality | Performance | Fanbase | 样本数 |
|-----|------------|------------|---------|-------|
| Athlete | 5 | 2 | 3 | 95 |
| Actor/Actress | 2| 5 | 3 | 128 |
| TV Personality | 1 | 3 | 5 | 67 |
| Singer/Rapper | 3 | 5 | 4 | 61 |
| Model | 3 | 4 | 3 | 17 |

**参数说明**：
- $\text{Physicality}$：体能/身体控制要求 (1-5)
- $\text{Performance}$：舞台表现力/镜头经验 (1-5)
- $\text{Fanbase}$：固有粉丝基础 (1-5)

---

#### 4.2.2 最优 K 值选择

**公式**（轮廓系数）：
$$ s = \frac{b - a}{\max(a, b)} $$

其中：
- $a$：簇内平均距离
- $b$：最近簇的平均距离

**K 值选择结果**：

| K | Inertia (SSE) | Silhouette Score | 推荐 |
|---|--------------|-----------------|------|
| 2 | 48.91 | 0.4067 | |
| **3** | **31.81** | **0.4595** | **✓ 最优** |
| 4 | 19.44 | 0.4472 | |
| 5 | 15.99 | 0.3916 | |

---

#### 4.2.3 行业聚类结果

| 簇名称 | Physicality | Performance | Fanbase | 样本占比 |
|-------|------------|------------|---------|---------|
| **Athletic Elite** | 4.17 | 2.00 | 2.83 | 24.6% |
| **Performance Artist** | 1.53 | 3.82 | 2.47 | 55.7% |
| **Fan Favorite** | 1.00 | 3.00 | 5.00 | 19.7% |

---

### 4.3 混合效应模型 (LMM)

#### 4.3.1 技术轨道 (Model A)

**公式**：
$$ \text{Score}_i = \beta_0 + \beta_1 \text{Cluster}_i + \beta_2 \text{Week}_i + \beta_3 (\text{Cluster} \times \text{Week})_i + \beta_4 \text{Age}_i + v_{\text{Partner}_i} + \epsilon_i $$

**参数说明**：

| 参数 | 定义 | 系数 | P值 | 显著性 |
|-----|-----|------|-----|--------|
| $\beta_0$ | Athletic Elite 基线 | 0.637 | <0.001 | *** |
| $\beta_1(\text{Fan Favorite})$ | Fan Favorite 效应 | -0.048 | <0.001 | *** |
| $\beta_1(\text{Performance Artist})$ | Performance Artist 效应 | 0.007 | 0.438 | |
| $\beta_2$ | 每周成长 | +0.030 | <0.001 | *** |
| $\beta_3(\text{Fan Favorite} \times \text{Week})$ | 追赶效应 | +0.004 | 0.035 | * |
| $\beta_4$ | 年龄效应 | -0.044 | <0.001 | *** |
| $v_{\text{Partner}}$ | 舞伴随机效应 | - | - | |

**关键发现**：
- 🏋️ **运动员优势**：Athletic Elite 起点最高
- 📉 **粉丝型劣势**：Fan Favorite 技术评分低 4.8%
- 📈 **成长效应**：每周技术提升 3%
- 🚀 **追赶效应**：Fan Favorite 成长更快（+0.4%/周）
- 👴 **年龄惩罚**：年龄每增加 1 个标准差，技术评分下降 4.4%

---

#### 4.3.2 人气轨道 (Model B)

**公式**：
$$ \log(\text{FanShare}_i) = \alpha_0 + \alpha_1 \text{Cluster}_i + \alpha_2 \text{Score}_i + \alpha_3 \text{Reputation}_i + \alpha_4 \text{Age}_i + v_{\text{Partner}_i} + \epsilon_i $$

**参数说明**：

| 参数 | 定义 | 系数 | P值 | 显著性 |
|-----|-----|------|-----|--------|
| $\alpha_0$ | 截距 | -1.957 | <0.001 | *** |
| $\alpha_1(\text{Fan Favorite})$ | 行业效应 | 0.048 | 0.465 | |
| $\alpha_1(\text{Performance Artist})$ | 行业效应 | 0.075 | 0.160 | |
| $\alpha_2$ | 技术评分效应 | -0.998 | <0.001 | *** |
| $\alpha_3$ | 初始名气效应 | +3.155 | <0.001 | *** |
| $\alpha_4$ | 年龄效应 | -0.182 | <0.001 | *** |

**关键发现**：
- 🌟 **名气主导**：初始名气是最强预测因子（+3.16）
- ⚖️ **行业无差异**：行业对粉丝投票无显著影响
- 🎭 **技术-人气悖论**：技术评分越高，粉丝投票越少（-1.0）
- 👴 **年龄惩罚加剧**：对人气影响是技术的 4 倍

---

### 4.4 年龄线性检验

**公式**：
$$ \text{Model}_{\text{linear}}: Y = \beta_0 + \beta_1 \text{Age} + \epsilon $$
$$ \text{Model}_{\text{quadratic}}: Y = \beta_0 + \beta_1 \text{Age} + \beta_2 \text{Age}^2 + \epsilon $$

**F 检验**：

| 赛道 | 线性 LogLik | 二次 LogLik | Age² P值 | 推荐 |
|-----|------------|------------|---------|------|
| 技术评分 | 1706.99 | 1703.13 | 0.0892 | 线性 ✓ |
| 粉丝投票 | -4304.39 | -4307.50 | 0.7029 | 线性 ✓ |

**结论**：两个模型的二次项 P 值均 > 0.05，线性假设成立。

---

### 4.5 盲投效应分析 (Blind Voting Effect)

#### 4.5.1 代理变量

$$ D_{\text{Blind}} = \begin{cases} 1 & \text{if Season} \in \{28, 29, 30\} \\ 0 & \text{otherwise} \end{cases} $$

**背景**：S28-S30 引入 Live Vote，但西海岸有 3 小时时差

---

#### 4.5.2 交互效应模型

**公式**：
$$ Y_{\text{Fan}} = \alpha_0 + \alpha_1 \text{Score} + \alpha_2 (\text{Score} \times D_{\text{Blind}}) + \alpha_3 \text{Rep} + \alpha_4 (\text{Rep} \times D_{\text{Blind}}) + \sum \alpha_k \text{Cluster}_k + v_{\text{Partner}} $$

**结果**：

| 参数 | 系数 | Std.Err | P值 | 解读 |
|-----|------|--------|-----|-----|
| $\alpha_1$ (Score) | -0.672 | 0.262 | 0.010** | 基准：分数高投票少 |
| **$\alpha_2$ (Score × D_Blind)** | **-3.188** | 0.831 | **<0.001*** | 🔥 盲投脱钩 |
| $\alpha_3$ (Reputation) | +2.955 | 0.342 | <0.001*** | 声誉驱动投票 |
| **$\alpha_4$ (Rep × D_Blind)** | **+3.063** | 1.192 | **0.010*** | 🔥 声誉替代 |

**核心发现**：
- ✅ **假设1验证**：盲投时期，评分对投票的影响大幅削弱（-3.19）
- ✅ **假设2验证**：盲投时期，历史声誉对投票的影响显著增强（+3.06）

---

### 4.6 专业舞伴效应 (Kingmaker Effect)

#### 4.6.1 技术提升 Top 3

| 舞伴 | 技术加成 |
|-----|---------|
| Derek Hough | +0.075 |
| Artem Chigvintsev | +0.055 |
| Valentin Chmerkovskiy | +0.042 |

#### 4.6.2 人气提升 Top 3

| 舞伴 | 人气加成 |
|-----|---------|
| Derek Hough | +0.123 |
| Jenna Johnson | +0.070 |
| Valentin Chmerkovskiy | +0.061 |

**结论**：Derek Hough 是唯一同时登顶技术和人气榜的"双料 Kingmaker"

---

## 5. Question 4：新赛制设计

### 5.1 动态门槛百分比模型 (DTPM)

**核心目标**：修复现有赛制的公平性漏洞

**设计理念**：
1. **初期严把关**：前几周评委掌握绝对话语权
2. **差生打折**：表现极差的选手粉丝票自动打折扣

---

### 5.2 机制 A：动态权重 (Dynamic Weighting)

**公式**：
$$ w(t) = w_{\text{start}} - \frac{t-1}{T-1} \times (w_{\text{start}} - w_{\text{end}}) $$

**参数说明**：

| 参数 | 定义 | 最优值 |
|-----|-----|-------|
| $w(t)$ | 第 $t$ 周的评委权重 | 动态变化 |
| $w_{\text{start}}$ | 初始评委权重 | 0.90 |
| $w_{\text{end}}$ | 最终评委权重 | 0.60 |
| $T$ | 赛季总周数 | 因赛季而异 |

**物理意义**：
- 第 1 周：评委掌握 90% 话语权
- 决赛周：评委掌握 60% 话语权
- 线性递减，"先严后松"

---

### 5.3 机制 B：表现门槛乘数 (Performance Gated Multiplier)

**公式**：
$$ \gamma_i = \begin{cases} 1.0 & \text{if JudgeScore}_i \geq 0.8 \times \mu_{\text{judge}} \\ \beta & \text{if JudgeScore}_i < 0.8 \times \mu_{\text{judge}} \end{cases} $$

**参数说明**：

| 参数 | 定义 | 最优值 |
|-----|-----|-------|
| $\gamma_i$ | 选手 $i$ 的表现乘数 | 1.0 或 0.4 |
| $\text{JudgeScore}_i$ | 选手 $i$ 当周评委分 | 原始数据 |
| $\mu_{\text{judge}}$ | 当周评委平均分 | 计算值 |
| $\beta$ | 惩罚系数 | 0.40 |

**逻辑**：
- 评委分 ≥ 80% 平均分：乘数 = 1.0（正常）
- 评委分 < 80% 平均分：乘数 = 0.4（打四折）

---

### 5.4 DTPM 总分公式

**公式**：
$$ \text{DTPM Score}_i = w(t) \times P_{\text{judge},i} + (1 - w(t)) \times (P_{\text{fan},i} \times \gamma_i) $$

**参数说明**：

| 参数 | 定义 |
|-----|-----|
| $\text{DTPM Score}_i$ | 选手 $i$ 的综合得分 |
| $w(t)$ | 动态评委权重 |
| $P_{\text{judge},i}$ | 选手 $i$ 的评委分占比 |
| $P_{\text{fan},i}$ | 选手 $i$ 的粉丝票 |
| $\占比gamma_i$ | 表现门槛乘数 |

**淘汰规则**：
$$ \text{Eliminated} = \arg\min_i (\text{DTPM Score}_i) $$

---

### 5.5 验证结果

#### 5.5.1 宏观指标对比

| 评估维度 | 历史现状 | 新赛制 (DTPM) | 优化效果 |
|---------|---------|--------------|---------|
| **公平性 (Kendall's Tau)** | 0.6248 | **0.6962** | 🟢 **提升 11.4%** |
| **混乱度 (Upset Rate)** | 56.47% | **24.78%** | 🟢 **降低 56.1%** |

**黄金区间定义**：15%-25% 的逆转率（保留悬念，但不至于"乱选"）

---

#### 5.5.2 争议案例修正

| 选手 (赛季) | 历史战绩 | DTPM 战绩 | 修正效果 |
|-----------|---------|----------|---------|
| **Bobby Bones (S27)** | 冠军 | 第 9 周淘汰 | 🚫 剥夺不公冠军 |
| **Billy Ray Cyrus (S4)** | 第 5 名 | 第 1 周淘汰 | 📉 提前 7 周 |
| **Bristol Palin (S11)** | 第 3 名 | 第 5 周淘汰 | 📉 提前 5 周 |
| **Jerry Rice (S2)** | 亚军 | 第 7 周淘汰 | 📉 提前 1 周 |

---

## 附录：关键指标定义

### A.1 解释率 (Explanation Rate)

$$ \text{Explanation Rate} = \frac{\text{成功复现的淘汰周数}}{\text{总消除周数}} \times 100\% $$

**含义**：模型能够解释多少历史淘汰结果

---

### A.2 置信度 (Certainty)

$$ \text{Certainty} = \frac{n_{\text{valid\_sims}}}{N_{\text{total}}} $$

**含义**：在有效模拟中，模型对预测的确信程度

---

### A.3 香农熵 (Shannon Entropy)

$$ H(X) = -\sum_{i=1}^{n} p_i \ln(p_i) $$

**含义**：衡量概率分布的不确定性（单位：nats）

---

### A.4 信息比率 (Information Ratio)

$$ \text{IR} = \frac{\text{Explanation Rate}}{1 - \text{Certainty}} $$

**含义**：在解释能力和自信度之间寻找最优平衡

---

### A.5 Kendall's Tau

$$ \tau = \frac{C - D}{\frac{1}{2}n(n-1)} $$

其中：
- $C$：和谐对数量
- $D$：不和谐对数量
- $n$：样本数量

**含义**：衡量两个排名之间的一致性（1 = 完全一致，-1 = 完全相反）

---

### A.6 轮廓系数 (Silhouette Score)

$$ s = \frac{b - a}{\max(a, b)} $$

**含义**：衡量聚类质量（-1 到 1，越高越好）

---

**文档维护**：
- 最后更新：2026-02-03
- 相关文件：`src/` 代码目录、`results/` 结果目录
- 如有疑问，请参考各模块的 `main()` 函数
